import { FormShopValidators } from './form-shop-validators';

describe('FormShopValidators', () => {
  it('should create an instance', () => {
    expect(new FormShopValidators()).toBeTruthy();
  });
});
